
uniform int IsDrawLine;
uniform vec4 vColor;
varying vec4 outColor;
void main()
{
    /* gl_FragColor = vec4(1,0,0,1); */
    /* gl_FragColor = vColor; */
    if(IsDrawLine == 1)
        gl_FragColor = vColor;
    else
        gl_FragColor = outColor;
};
